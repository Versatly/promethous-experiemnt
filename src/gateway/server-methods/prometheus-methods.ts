export const PROMETHEUS_GATEWAY_READ_METHODS = [
  "prometheus.status",
  "prometheus.trajectory",
  "prometheus.goals",
  "prometheus.recursion",
  "prometheus.autarch",
  "prometheus.monolith",
] as const;
